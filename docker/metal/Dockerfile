# syntax=docker/dockerfile:1
# Sovereign Runtime image: metal-arm64 profile.
#
# Phase 1 (MVP conformance): identical appliance + vLLM CPU backend on
# aarch64; manifest reports apple/CPU execution honestly (never claims Metal).
# Phase 2: appliance engine backend swapped for the host inference agent
# client (design.md §2.6; contract unchanged).
#
# TODO(M11): derive from docker/cpu once that base exists.

FROM python:3.12-slim
RUN echo "TODO(M11): metal-arm64 phase 1 image" && exit 1
