# syntax=docker/dockerfile:1
# Sovereign Runtime image: cpu-x86_64 / cpu-arm64 profile (bring-up, dev,
# and the base for Metal Phase 1).
# Build context: repository root — docker build -f docker/cpu/Dockerfile .
#
# TODO(M3): pick the upstream vLLM CPU base (or build vLLM CPU from source
# for linux/amd64 + linux/arm64) and layer the appliance as in docker/cuda.

FROM python:3.12-slim
RUN echo "TODO(M3): vLLM CPU base image" && exit 1
